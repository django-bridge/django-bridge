[tool.black]
# If you update these settings, please update the black args in wagtail-cloud.code-workspace as well
line-length = 80
target-version = ['py38']

[tool.isort]
profile = 'black'
known_first_party = ['appshell', 'example']
